/** Floating panel width plus its inset; the work area frames around it. */
export const PANEL_INSET = 344;

/** The same strip plus a gap, so floating chrome never touches the panel. */
export const PANEL_STRIP = PANEL_INSET + 24;
